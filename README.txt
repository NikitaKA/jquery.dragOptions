
jQuery Draggable Select Options

Use with native selects. Sort list of options by drag'n'drop.

Supperted Browsers: Chrome, Firefox, Opera, Safari.
                    Internet Explorer (any) not supported due to "problems" with <option> object :( Thanks Microsoft.

Usage:
$("select").dragOptions({});

Options:
highlight: String  //Symbol to highlight current draggable option